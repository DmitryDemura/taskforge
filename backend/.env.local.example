# Local Development Configuration
# Use this file when running backend locally (not in Docker)
# Copy to .env.local and modify as needed

# Database Configuration - localhost with alternative port to avoid conflicts
DATABASE_URL="postgresql://taskforge:taskforge@localhost:5433/taskforge"

# Redis Configuration - localhost with alternative port to avoid conflicts  
REDIS_URL="redis://localhost:6380"

# API Configuration
PORT=2999
NODE_ENV=development

# Notes:
# - This configuration assumes you're running DB and Redis via docker-compose.local.yml
# - Alternative ports (5433, 6380) are used to avoid conflicts with local installations
# - The backend will run locally with Windows Prisma Client support
# - Use: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d db redis
