# Override for local development
# Use this when running backend locally but need Redis from Docker
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up redis

services:
  redis:
    ports:
      - '6380:6379'
